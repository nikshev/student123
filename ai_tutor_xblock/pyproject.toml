[project]
name = "ai-tutor-xblock"
version = "0.1.0"
description = "AI Tutor XBlock for Open edX — unit-level AI tutor chat and interaction"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project.entry-points."xblock.v1"]
ai_tutor_xblock = "ai_tutor_xblock.block:AiTutorXBlock"

[tool.setuptools.packages.find]
include = ["ai_tutor_xblock*"]

[tool.setuptools.package-data]
ai_tutor_xblock = ["static/**/*", "templates/**/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "unit: Unit tests",
    "integration: Integration tests",
    "contract: Contract tests",
]
addopts = "-p no:warnings"
filterwarnings = [
    "ignore::DeprecationWarning",
]